import { LaravelPaginatorService } from './laravel-paginator-service';

describe('LaravelPaginatorService', () => {
  it('should create an instance', () => {
    expect(new LaravelPaginatorService()).toBeTruthy();
  });
});
